schema: catalog.nkp.nutanix.com/v1/application-metadata
displayName: Let's Encrypt ClusterIssuer
allowMultipleInstances: false
category:
  - dm-nkp-gitops-app-catalog
  - security
  - certificates
  - infrastructure
description: |
  Let's Encrypt ClusterIssuer configurations for cert-manager. Provides both staging
  and production ACME issuers for automated TLS certificate provisioning using
  Let's Encrypt. Deploy after cert-manager.
dependencies: []
icon: ""
licensing:
  - Pro
  - Ultimate
overview: |
  **What it is** — Pre-configured ClusterIssuer resources for Let's Encrypt so cert-manager can automatically provision TLS certificates via ACME.

  **Highlights**
  - letsencrypt-staging: for testing (not trusted by browsers)
  - letsencrypt-prod: for production (trusted certificates)
  - Use with cert-manager and Ingress for automatic HTTPS (e.g. HTTP-01 challenge)

  **Documentation:** [cert-manager ACME](https://cert-manager.io/docs/configuration/acme/) | **Project:** [Let's Encrypt](https://letsencrypt.org/)
scope:
  - workspace
supportLink: https://cert-manager.io/docs/configuration/acme/
type: custom
